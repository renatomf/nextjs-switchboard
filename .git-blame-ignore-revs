# Commits that only reformat code, so `git blame` points past them to the
# change that actually wrote each line. GitHub reads this file on its own;
# locally, run once: git config blame.ignoreRevsFile .git-blame-ignore-revs

# style: format codebase with Prettier
9577c1f14bc06ea171c633d512d09a7a4108fa24
